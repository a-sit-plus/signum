<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <provider
            android:name="at.asitplus.signum.supreme.InitProvider"
            android:authorities="${applicationId}.SignumSupremeInitProvider"
            android:exported="false"
            android:enabled="true"/>
    </application>
</manifest>