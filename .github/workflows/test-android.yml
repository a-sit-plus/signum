name: Test Android implementation
on:
  # still run checks on every PR *before* the merge
  pull_request:

  # run the workflow once more **after** the PR has been merged
  push:
    branches:
      - main
      - development

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout (default)
        if: github.event_name != 'pull_request'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      # For pull_request_target, explicitly fetch the PR head
      - name: Checkout PR head (for pull_request)
        if: github.event_name == 'pull_request'
        uses: actions/checkout@v4
        with:
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
          submodules: recursive
      - uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - name: Connected Check
        run: ./gradlew pixel2api36AndroidDeviceTest
      - name: 'Add default settings to node calls'
        shell: bash
        run: |
          #
          # The later used dorny/test-reporter@v2 action can throw the following exception when enough tests have been
          # executed:
          #     RangeError: Maximum call stack size exceeded
          #
          # We explicitly increase the max. stack size here to workaround this issue.
          #
          
          which node
          node --version
          
          mv /usr/local/bin/node /usr/local/bin/node_org
          echo '#!/bin/bash' >> /usr/local/bin/node
          echo '/usr/local/bin/node_org --stack-size=2048 $@' >> /usr/local/bin/node
          cat /usr/local/bin/node
          chmod +x /usr/local/bin/node
      - name: Test Report
        uses: dorny/test-reporter@v2
        env:
          NODE_OPTIONS: --max-old-space-size=4096
        if: success() || failure()
        with:
          name: Android Device Tests
          path: indispensable-asn1/build/outputs/androidTest-results/managedDevice/androidmain/**/TEST*.xml,indispensable-oids/build/outputs/androidTest-results/managedDevice/androidmain/**/TEST*.xml,indispensable/build/outputs/androidTest-results/managedDevice/androidmain/**/TEST*.xml,indispensable-josef/build/outputs/androidTest-results/managedDevice/androidmain/**/TEST*.xml,indispensable-cosef/build/outputs/androidTest-results/managedDevice/androidmain/**/TEST*.xml,supreme/build/outputs/androidTest-results/managedDevice/androidmain/**/TEST*.xml
          reporter: java-junit
          use-actions-summary: true